# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# Dev-only assets (NOT shipped, NOT in production zip)
_seed.html
shot-*.png

# Build artifacts (regenerated by build_zip.py)
my-app_*.zip
build_zip.py

# Node / npm (only if you decide to add tooling later)
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml
